an html element requires a starting tag and an ending tag <h1> -------</h1>
an html element requires a selfclosing tag <br> or <hr>
an html element can have attribute <hr size=3>
--list of sources of HTML tags and elements
--tag
head
title
body

--self closing tag like <br>
--how do we know which tag requires / does not require a closing tag --ref --DevDocs API Documentation

--elements
Heading HTML MDN
W3Schools HTML Headings 
DevDocs API Documentation

--attributes
--size

linter --inter dependency for linter-eslint
linter-ui-default depends on intentions, busy-signal